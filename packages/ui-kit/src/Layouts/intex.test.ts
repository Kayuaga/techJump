import { jest } from '@jest/globals'


describe('Ui kit test', () => {
    beforeEach(() => {

    })

    it('modal testing', () => {
        expect('modal').toBe('modal')
    })

    it('modal two testing', () => {
        expect('two').toEqual('two')
    })
})
